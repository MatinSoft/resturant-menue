برای toast messages که از Django می‌آیند، باید توجه داشته باشید که این پیام‌ها از قبل ترجمه شده‌اند (Django سمت سرور ترجمه را انجام می‌دهد). بنابراین نیازی به ترجمه مجدد آنها در فرانت‌اند نیست. فقط کافی است بررسی کنید که خالی یا "none" نباشند:


چرا الان با تغییر زبان صفحه به عربی متن toast ها عربی نمیشود مانند پیام های login welcome
مشکل اینجاست که t در handleFinalizeOrder یک closure است و مقدار آن در زمان تعریف تابع ثابت می‌ماند. وقتی زبان تغییر می‌کند، t آپدیت می‌شود اما handleFinalizeOrder همچنان به t قدیمی اشاره می‌کند.

راه حل: استفاده از useCallback با وابستگی به t
در App.jsx، تابع handleFinalizeOrder را با useCallback تعریف کنید: